# Copy this to .env.local and adjust if your backend isn't on localhost:8000.
# Must be prefixed NEXT_PUBLIC_ to be readable in the browser — see lib/api.ts.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
